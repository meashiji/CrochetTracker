[project]
name = "crochet-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.136.1",
    "uvicorn>=0.48.0",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
]
